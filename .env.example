# Agntor Trust Protocol — Environment Configuration
# Copy this to .env and fill in values

# Deployer private key (NEVER commit the real key)
PRIVATE_KEY=0x_your_private_key_here

# BSCScan API key for contract verification
BSCSCAN_API_KEY=

# Gas reporting
REPORT_GAS=false

# Deployed contract address (filled after deployment)
CONTRACT_ADDRESS=

# RPC overrides (optional, defaults in hardhat.config.ts)
BSC_TESTNET_RPC=https://data-seed-prebsc-1-s1.bnbchain.org:8545
OPBNB_TESTNET_RPC=https://opbnb-testnet-rpc.bnbchain.org
